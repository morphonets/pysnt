# Documentation build requirements for Read the Docs
# Using older, stable versions to avoid timezone initialization issues

# Core Sphinx and extensions
sphinx>=4.0,<6.0
sphinx-design
sphinx-copybutton
pydata-sphinx-theme

# Notebook support with conservative version pinning
myst-nb>=0.13.0,<0.18.0

# Use older, stable Jupyter versions that don't have timezone issues
jupyter-client>=6.1.12,<7.0.0
nbclient>=0.5.13,<0.6.0
jupyter-cache>=0.4.3,<0.5.0

# Additional utilities
setuptools>=61.0
wheel