# Documentation build requirements for Read the Docs
# Minimal dependencies with targeted timezone fix for Jupyter

# Core Sphinx and extensions
sphinx>=4.0
sphinx-design
sphinx-copybutton
pydata-sphinx-theme

# Notebook support with specific version pinning to avoid timezone issues
myst-nb>=0.17.0,<1.0.0

# Pin Jupyter dependencies to stable versions
jupyter-client>=7.4.0,<8.0.0
nbclient>=0.6.0,<0.8.0

# Additional utilities
setuptools>=61.0
wheel