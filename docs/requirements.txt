# Documentation build requirements for Read the Docs

# Core Sphinx and theme
sphinx>=4.0,<8.0
pydata-sphinx-theme>=0.13.0

# MyST and notebook support
myst-nb>=0.17.0
myst-parser>=0.18.0

# Sphinx extensions
sphinx-design>=0.3.0
sphinx-copybutton>=0.5.0

# Minimal dependencies for package import (mocked in conf.py)
# We mock the heavy dependencies to avoid installation issues on RTD