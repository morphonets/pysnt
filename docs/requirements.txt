# Documentation build requirements for Read the Docs
# Keep this minimal: we mock the heavy dependencies to avoid installation issues on RTD

# Core Sphinx and extensions
sphinx>=4.0
sphinx-design
sphinx-copybutton
pydata-sphinx-theme

# Notebook support with timezone fix
myst-nb>=0.17.0

# Pin specific versions to avoid timezone issues
jupyter-client>=7.0.0,<8.0.0
nbclient>=0.5.0,<0.8.0

# Additional utilities
setuptools>=61.0
wheel